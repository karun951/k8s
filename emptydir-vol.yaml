apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubuntu-deploy
  namespace: alpha
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ubuntu
  template:
    metadata:
      labels:
        app: ubuntu
    spec:
      containers:
      - name: ubuntu-container
        image: ubuntu
        command: ["/bin/sleep", "infinity"]
        volumeMounts:
        - name: emptydir-storage
          mountPath: /tmp/data
      volumes:
      - name: emptydir-storage
        emptyDir: {}
